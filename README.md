JavaForge is a lightweight web-based Java IDE that allows users to create Java projects, packages, classes, interfaces, enums, records, and annotations, edit code in the browser, compile and run programs with interactive console input, and download projects as Maven-ready ZIP files. Built with Spring Boot, Thymeleaf, CodeMirror, and WebSockets, it provides isolated guest workspaces for multiple concurrent users without requiring login or local installation.
